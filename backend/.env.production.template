# ============================================
# Production Environment Variables Template
# ============================================
# Copy this file and set these environment variables before deploying to production
# DO NOT commit actual production values to Git!
# ============================================

# ============================================
# Database Configuration
# ============================================
DATABASE_URL=jdbc:mysql://your-production-db-host:3306/crud_db?useSSL=true&serverTimezone=UTC
DATABASE_USERNAME=your_db_username
DATABASE_PASSWORD=your_secure_db_password

# ============================================
# JWT Security Configuration
# ============================================
# Generate a strong 256-bit secret key using:
# openssl rand -base64 64
JWT_SECRET=your-very-long-and-secure-secret-key-at-least-256-bits-long
JWT_EXPIRATION_MS=86400000
ADMIN_SECRET=your-secure-admin-secret-key

# ============================================
# CORS Configuration
# ============================================
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ============================================
# Server Configuration
# ============================================
SERVER_PORT=8082

# ============================================
# File Upload Configuration
# ============================================
UPLOADS_DIR=/var/app/uploads
MAX_FILE_SIZE=5MB
MAX_REQUEST_SIZE=5MB
MAX_IMAGE_SIZE=5242880

# ============================================
# Spring Profile Activation
# ============================================
SPRING_PROFILES_ACTIVE=prod

# ============================================
# How to Use (Linux/Mac):
# ============================================
# 1. Copy this file: cp .env.production.template .env.production
# 2. Edit .env.production with your actual values
# 3. Source the file: source .env.production
# 4. Run application: java -jar target/demo-0.0.1-SNAPSHOT.jar

# ============================================
# How to Use (Windows PowerShell):
# ============================================
# $env:DATABASE_URL="jdbc:mysql://..."
# $env:DATABASE_USERNAME="username"
# $env:DATABASE_PASSWORD="password"
# ... (set all variables)
# java -jar target/demo-0.0.1-SNAPSHOT.jar

# ============================================
# How to Use (Docker):
# ============================================
# Create a .env file with these variables and use:
# docker run --env-file .env.production -p 8082:8082 your-image

# ============================================
# Security Notes:
# ============================================
# - NEVER commit this file with actual values to Git
# - Use a secrets manager (AWS Secrets Manager, Azure Key Vault, etc.) in production
# - Rotate JWT secrets regularly
# - Use strong passwords for database and admin access
# - Enable SSL/TLS for database connections
# - Use HTTPS for your application in production
